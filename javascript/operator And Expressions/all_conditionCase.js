// conditional operator
// if else , nested if else 
 /*
 if(some condition) {
  console.log(" the staetment")
 }
 else{
  console.log("the false statement")
 } 
 */

// Logical operator
//  && , || , != etc use  in maximum  conditional condtion


 // switch operator 
 /*
 let color = red 
 switch (c==) {
  case value:
    
    break;
 
  default:
    break;
 }
  */